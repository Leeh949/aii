1. oracl XE setup후 cmd 창을 띄워 다음 실행
2.  관리자계정으로 접속한 후, 계정생성

c:\~> sqlplus system/비번 - 관리자
        splplus"as sysdba - 최상위권한

SQL> create uesr scott identified by tiger;
SQL> grant dba to scott;

3. 관리자 계정 접속 해제
SQL> exit;

4. scott계정으로 접속 후, 수업시간 쓸 데이터 넣기 (demo.sql)

sqlplus scott/tiger


SQL> SELECT * FROM TAB;

SQL> @C:\aii\lecNote\04_DMBS/demo.sql;

SQL> select * from dept;

6. sqlDeveloper 다운로드 압출풀어씀



